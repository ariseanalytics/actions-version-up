name: Tag
description: Update git tags
inputs:
  what-to-update:
    description: 'What to update'
    required: true
    default: 'revision'
runs:
  using: docker
  image: Dockerfile
  args:
    - ${{ inputs.what-to-update }}
